# AGENTS.md

本文件用于指导 AI 代理（及协作者）在本仓库中的工作规范。

## Spec 驱动开发（Spec-Driven Development）

本项目采用 Spec 驱动开发方式，**一切开发以 Spec（规格说明）为准**。

1. **先 Spec，后代码**
   - 任何功能开发、行为变更或缺陷修复，都应先编写或更新对应的 Spec，再实现代码。
   - Spec 应描述预期行为、输入输出与验收要点，作为开发与验证的依据。

2. **提交前必须维护 Spec**
   - 每次执行 `git commit` 之前，必须检查并维护 Spec，确保 Spec 与本次代码改动保持同步：
     - 新增功能 → 补充对应的 Spec；
     - 修改行为 → 更新对应的 Spec；
     - 移除功能 → 删除或废弃对应的 Spec。
   - 提交内容中应包含对应 Spec 的维护改动；仅改代码而未同步 Spec 的提交不被允许。

3. **Spec 与测试对应**
   - 测试应能追溯到 Spec；Spec 中描述的验收要点应有相应测试进行验证。

## 提交（Commit）规范

1. **提交前必须测试和验证**
   - 每次执行 `git commit` 之前，必须运行相关测试并对改动进行验证，确保改动可正常工作、不破坏已有功能。
   - 验证通过后，方可提交。

2. **提交备注必须使用中文**
   - 每次 commit 的备注（message）必须使用中文撰写，简洁、清晰地描述本次改动的内容。
   - 推荐格式：`类型: 简述`（示例：`修复: 修正登录页面样式错乱`、`新增: 添加用户注册功能`）。

3. **其他约定**
   - 提交应保持粒度合理：一次提交只包含逻辑上相关的改动，避免混入无关修改。
   - 涉及重大变更或破坏性改动时，应在备注中予以说明。
