# AGENTS.md

本文件用于指导 AI 代理（及协作者）在本仓库中的工作规范。

## 提交（Commit）规范

1. **提交前必须测试和验证**
   - 每次执行 `git commit` 之前，必须运行相关测试并对改动进行验证，确保改动可正常工作、不破坏已有功能。
   - 验证通过后，方可提交。

2. **提交备注必须使用中文**
   - 每次 commit 的备注（message）必须使用中文撰写，简洁、清晰地描述本次改动的内容。
   - 推荐格式：`类型: 简述`（示例：`修复: 修正登录页面样式错乱`、`新增: 添加用户注册功能`）。

3. **其他约定**
   - 提交应保持粒度合理：一次提交只包含逻辑上相关的改动，避免混入无关修改。
   - 涉及重大变更或破坏性改动时，应在备注中予以说明。
